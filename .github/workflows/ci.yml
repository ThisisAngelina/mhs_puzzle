name: Testing
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Django unit tests
      - run: 
        pip3 install --user django #TODO fix the syntax error
        pip3 install --user -r requirements.txt
        python3 manage.py test
